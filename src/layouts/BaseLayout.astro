---
// Components
import { Footer } from "@components/Organisms/Core/Footer/Footer";
import { NavBar } from "@components/Organisms/Core/NavBar/NavBar";

// Content
import strings from "@content/strings.json";

// Css and fonts
import "@styles/index.scss";
import "@fontsource-variable/open-sans";

// Helpers
import { fetchStory } from "@helpers/fetchContent";
import type { FooterProps } from "@components/Organisms/Core/Footer/Footer.interface";
import type { NavBarProps } from "@components/Organisms/Core/NavBar/NavBar.interface";

interface Props {
  title?: string;
}
const { title } = Astro.props;

let footerContent = null;
let navBarContent = null;

try {
  const footerResponse = await fetchStory("footer");
  footerContent = footerResponse.data?.story?.content as FooterProps;
} catch (error) {
  console.error("Failed to fetch Footer story:", error);
}

try {
  const navBarResponse = await fetchStory("navbar");
  navBarContent = navBarResponse.data?.story?.content as NavBarProps;
} catch (error) {
  console.error("Failed to fetch NavBar story:", error);
}
---§  1q


<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      defer
      is:inline></script>
    <title>{title ? `${strings.title} | ${title}` : strings.title}</title>
  </head>
  <body class="d-flex flex-column min-vh-100">
    <header>
      {navBarContent && <NavBar {...navBarContent} client:load />}
    </header>
    <main class="flex-grow-1">
      <slot />
    </main>
    {footerContent && <Footer {...footerContent} client:load />}
  </body>
</html>
